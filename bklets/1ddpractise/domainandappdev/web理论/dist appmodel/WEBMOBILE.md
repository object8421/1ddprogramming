WEB,mobile

WEB不是云，也不是浮云

曾几何时，我们直接把WEB当成了我们需要的那朵云
诚然，企业靠WEB实现了它们大多数业务需求。。分布式开发似乎非WEB莫属。
可是，如果翻开历史，就可以看到，，最初的web仅仅是为了分享资源，促进教育
。
html的发明就是最初那个WEB，，，被人们称为静态WEB的那个年代，WEB只是展示
和分享用的东西。。
后来动态WEB出现了，，，它就成了一种“WEB平台的东西”
我们知道平台这个词是重量的，，它至少是应用的平台，开发的平台，发布的平台（组件平台）。。
1,硬件平台上，应用，开发，发布，，都是以PC为主体的。
2，软件平台上（有OS的平台），以上三者都是以应用程序[来源：GameRes.com]为主体的。
3，分布式平台上（有网络的平台），以上三者都是以虚拟机为主体的。
1，2比较好理解，3需要归纳一下。《请看我的QT救赎下的WEBGAME》

在现今阶段，，我们所指的云，，主要是WEB，而云，，它的广义，，是指通用分布式平台（应用，开发，发布），，而不仅仅是我们已经做得很好的那套ＷＥＢ。
于是，我们误用了太多东西，，，webgame,,web xx ,,web all，即使它们有时仅是表面上工作得正常。。
－－－－－－－－－－－－－－－－－－－－－－－－－－－－－－－－－－
需要出现一种全新的架构，比如，以组件为基础的分布式平台来统一云计算（比如.net vm,java中的enterprise
beans），重新改良现有ＷＥＢ架构的Ｂ／Ｓ部分，使之容纳通用的云计算应用。

协议工程与通用WEB浏览器

在WEB开发中，GUI被做成了HTML之类的语义，DB被做成了SQL之类的语义，大凡涉及到语义，都已经直接进入程序如何被应用，以适应工程需要的范畴了，所以，它一般很高阶，
对于网络，，就是协议工程了。。。协议，包括二个方面，1，二端如何交谈，2，二端交谈什么，，，1就是协议格式，二就是协议包(当协议被反工程时，进入到物理层被发送时，，除了必须的协议格式往来，，就是协议包中的数据实体了)中的数据。
具体的协议有TCP，UDP，HTML，SOAP，RTP，FTP，SNMP，，之类，但是这些协议其实并不是并列的，高阶的协议比如FTP，HTTP往往基于TCP，UDP这二个协议（至于网络层以下的协议比如IP协议作为基建就不直接被使用了，所以协议是网络实现的内容，又是[来源：GameRes.com]网络编程的模型—socket需要新的协议来实现新的传输规范和传输方式，可见。，网络编程领域是一个实现与开发，艺术搭建相结合的典型，这其中，以协议为中心），高阶的协议就带有面向连接，有无状态之类的特性了。比如，一般地当谈到面向链接时就是面向TCP。

近年来，随着WEB的兴起，出现了越来越多的协议，比如SOAP，及JSON封装，，这些协议，都有一个鲜明的特点，就是采用了XML描述，，这就使协议设计工程上升到通用协议格式的层次。
>>http，顾名思议，它善于传输文本，XML就是一种元文本格式，这就使SOAP之类的新HTTP协议，成为通用WEB协议了。
WEB是一个B/S架构的开发与应用架构，，在B端，WEB
explorer，以http为主导，各种协议相互合作，被分散在页面链接时，当点击一个FTP链接时，会启用FTP协议，点击RTP时会启用实时传输，，当启用HTTPS时会启用安全传输。这些都需要在server端被支持。
———————————————
我在《webgame的正确出路》中谈到，将越来越多的本地应用cloud化，必须为它们设置越来越多的servers，使webserver不仅仅是htmlservers,dbservers,等等东西，比如要做webgame，为了解决大型网游文件系统繁杂，画面3D需要的特点。真正的WEBgame应设置一个filesystemserver.
那么，其协议应是什么样的呢？（假如它叫simple filesystem server）
1，协议格式中应包括filelist ,比如当进入一个新地图时，地图scene包引用了多少文件，media
filesystem服务端会发送给客户端一个simple filesytem server协议。
,2，传输的数据中，不必要求是流媒体格式，只要在后台下载就可了，甚至可以用HTTP。以使webgmae达到一种微端的效果

较WEB，移动开发只是PC开发的副产品

pc是平台，mobile是平台，web也是平台，都有自己的一套应用，开发，发布规范，，mobile是移动，web是分布，，都是用解决PC的那一套方法来解决的（以PC技术代表的计算机科学和编程科学为中心）。
关于WEB平台，请参照我的《qt救赎下的webgame client player》与《WEB不是云，也不是浮云》
而这里要说到的移动平台是一类特殊的平台，CPU精简，有时需要实时，mobile平台的应用不限于移动电话,比如,也有电视机anriod
tv box，移动开发，最初采取的是PC上的方案，，比如sybiam,这就是运用QT，直接在natives上作文章。
>>为什么说mobile强烈采用PC上的解决方案呢，，我们注意到，它的os往往是某种linux,它的系统组件，与应用组件，直接采用PC上能工作得很好的，声望很好的一些库，如sqlite,sdl.gtk,jscore
到后来，移动平台主要作为web客户端，云计算应用客户端了，web平台的开发强调虚拟机编程，于是在mobile系统的系统组件中引入了jvm,,.net
cf这样的东西。sybiam
os中运用的QT直接在natives上作文章的开发架构，，就慢慢被舍弃了。

