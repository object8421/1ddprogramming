js，js没有语言级别的显式OO。而且，它有函数式（不考虑ＣＰＰ的模板机制带来的函数式元编程），它的函数式是可以用过程式解读的，二者基本同源，函数是用过程生成的，它相当于lua+py

dotnet，，相当于ＱＴ，使用了一套独特的ＯＯＡ，ＯＯＤ核心。比如delegate


js中，函数和过程，，都是表达式统一推导出来的，，然而，它是语言级的表达式，直接在语法树上写程序的那一类语言，，，通常表达式，是编译原理parse,interpter级的东西



新发明的语言，一般走的都是以上的路子


**由于self是函数语言。函数语言本身就是天然的表达式编译程序**。故用self实现的js在语言级所呈现出来的种种高级机制比如对象机制。都可以被解释为过程式的表达式。

js其实就是用语法级的self写出的语言级的c。或者说objectivec.用这个眼光解读js.基本能理解它的任何元素

比如js的基于对象。（js的原生类型只有六种。对象其实是函数在语言语法级求值表达出来的）。对象只是一种特殊的函数数据结构体（不要称它为函数对象。那样反而造成混乱）。它包含一个原型作为该数据结构的字段。这个原型有个构造器（它只是函数。仍然不要为它强加任何oo相关的意义）。

*/