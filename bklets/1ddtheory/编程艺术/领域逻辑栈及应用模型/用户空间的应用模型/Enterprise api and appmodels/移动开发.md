这就导出了web os,现今有三种web os,palm os,iphone,anriod

而且，移动开发界也存在着better c runtime langs和soft vm langs花开二支的现象，iphone的架构是better c runtime的objective c,,,而android是soft vm runtime的java(java由WEB开发转型为移动开发类语言),请问,who deals with the essials?? 

ios等webos的架构中，mysql等是作系统组件（就像传统OS的桌面环境组件）而存在的，它们是WEBOS中的compent,而不是APP

Web os下，不提倡对作为组件的qt,kde,mysql等的native开发，因为web os出现的目的，就是假设本地开发已稳定，因此web os的developer space就是非本地开发的，基于.net,jvm,mono之上的开发。而且将这种开发做到极大化。
